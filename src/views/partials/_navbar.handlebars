<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
    <div class="container">
        <a class="navbar-brand" href="/">Tienda Bicicletas</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav mx-auto">
                <a class="nav-link active" aria-current="page" href="/">Home</a>
                <a class="nav-link" href="/bicicletas/new">Crear Bicicletas</a>
                <a class="nav-link" href="/compras">Compras</a>
            </div>
            <div class="navbar-nav ms-auto">
                {{#if usuario}}
                    <span class="nav-link">Bienvenido: {{usuario.nombre}} - Rol: {{usuario.rol}}</span>
                    <a class="nav-link" href="/auth/logout">Cerrar Sesión</a>
                {{else}}
                    <a class="nav-link" href="/auth/login">Iniciar Sesión</a>
                    <a class="nav-link" href="/auth/register">Registrarse</a>
                {{/if}}
            </div>
        </div>
    </div>
</nav>